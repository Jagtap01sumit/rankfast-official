import styled from "styled-components";
import { motion } from "framer-motion";

export const MarkeeDiv = styled(motion.div)`
  font-family: var(--font-primary);
  color: white;
  z-index: 1;
  margin: 0;
  white-space: nowrap;
  margin-top: auto;
  transition: all 0.1s ease-in-out;
`;

export const MarkeeDiv2 = styled(motion.div)`
  font-family: var(--font-primary);
  color: white;
  z-index: 1;
  margin: 0;
  white-space: nowrap;
  margin-top: auto;
  transition: all 0.1s ease-in-out;
`;

